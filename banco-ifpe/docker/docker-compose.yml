services:
<<<<<<< HEAD
=======
<<<<<<< HEAD
>>>>>>> feature/constantes

  postgres:
    image: postgres:15
    container_name: postgres_banco_ifpe
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  builder:
    image: maven:3.9.6-eclipse-temurin-17
    container_name: builder_banco_ifpe
    working_dir: /app
    volumes:
      - ../:/app
      - maven_cache:/root/.m2
    command: mvn clean package

  wildfly:
    build: ./wildfly
    container_name: wildfly_banco_ifpe
    ports:
      - "8080:8080"
      - "9990:9990"
    volumes:
      - ../target/banco-ifpe.war:/opt/jboss/wildfly/standalone/deployments/banco-ifpe.war
      - wildfly_xml_history:/opt/jboss/wildfly/standalone/configuration/standalone_xml_history
    depends_on:
      postgres:
        condition: service_healthy
      builder:
        condition: service_completed_successfully

volumes:
  maven_cache:
  wildfly_xml_history:
<<<<<<< HEAD
=======
=======
  postgres:
    image: postgres:16-alpine
    container_name: db_banco_ifpe
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-banco_ifpe}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 10

  wildfly:
    image: quay.io/wildfly/wildfly:34.0.1.Final-jdk17
    container_name: wildfly_banco_ifpe
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080"
    volumes:
      - ../target/banco-ifpe.war:/opt/wildfly/standalone/deployments/banco-ifpe.war:ro
>>>>>>> 348a2033cffe9adbbaaf5e442f0b3298be3af1e2
>>>>>>> feature/constantes
